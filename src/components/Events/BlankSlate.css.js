import styled from 'styled-components'

import theme from '../../util/theme'

export default styled.div`
  color: ${theme.light.textColor.secondary};
  font-family: ${theme.base.fontFamily.sans};
  font-size: 1rem;
  text-align: center;
`
